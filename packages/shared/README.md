# @seo-autopilot/shared

Shared foundation for SEO AutoPilot:

- Event Bus (`InMemoryEventBus`) + platform event names
- Structured engine logging
- Engine configuration provider
- `EngineResult` envelope

/**
 * @seo-autopilot/web
 *
 * Sprint 0: application shell placeholder only.
 * No dashboard, no reports, no authentication UI, no design decisions.
 */
export const WEB_APP_STATUS = "scaffold-only" as const;

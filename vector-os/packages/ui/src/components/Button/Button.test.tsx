/**
 * Button test suite — placeholder until vitest is configured for @vector-platform/ui.
 *
 * TODO: Add vitest + @testing-library/react to packages/ui, then implement:
 * - renders children
 * - applies variant, size, and fullWidth classes
 * - sets aria-busy when loading
 * - disables when disabled or loading
 * - renders iconLeft and iconRight slots
 */

export const buttonTestCases = [
  'renders children',
  'applies variant classes',
  'applies size classes',
  'sets aria-busy when loading',
  'disables when disabled or loading',
  'renders iconLeft and iconRight',
  'applies fullWidth class',
] as const;

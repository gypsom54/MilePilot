{
  "name": "vector-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Vector Platform — pnpm + Turborepo monorepo",
  "scripts": {
    "dev": "pnpm --filter @vector-platform/website dev",
    "dev:storybook": "pnpm --filter @vector-platform/storybook dev",
    "build": "turbo run build",
    "build:safe": "pnpm --filter @vector-platform/tokens build && pnpm --filter @vector-platform/types build && pnpm --filter @vector-platform/utils build && pnpm --filter @vector-platform/icons build && pnpm --filter @vector-platform/ui build && pnpm --filter @vector-platform/website build",
    "typecheck": "turbo run typecheck",
    "typecheck:safe": "pnpm -r typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean --continue && rimraf node_modules/.cache"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "turbo": "^2.5.4",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}

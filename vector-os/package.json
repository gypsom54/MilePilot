{
  "name": "vector-os",
  "version": "0.1.0",
  "private": true,
  "description": "Vector OS — internal UI Lab and design system monorepo",
  "scripts": {
    "dev": "pnpm --filter @vector-os/website dev",
    "dev:storybook": "pnpm --filter @vector-os/storybook dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}

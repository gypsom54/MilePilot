# Resend setup

Domain is milepilot.uk.

Use sender:
reports@milepilot.uk

Backend `.env`:

PORT=8787
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=resend
SMTP_PASS=YOUR_NEW_RESEND_API_KEY
SMTP_FROM="MilePilot <reports@milepilot.uk>"

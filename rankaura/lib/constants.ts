/**
 * @deprecated Import from `@/config` instead. Kept for backward compatibility.
 */
export {
  APP_NAME,
  APP_TAGLINE,
  NAV_ITEMS,
  PLACEHOLDER_BUSINESS,
  PLACEHOLDER_USER,
} from "@/config";

# Keep EAS upload tarball small and avoid scanning unrelated paths on Windows.
.git
node_modules
*.zip
milepilot-upload-v2
.cursor
terminals
**/.DS_Store
**/Thumbs.db

# Windows — never upload user profile / temp paths (causes casing errors on EAS)
AppData
**/AppData/**
**/Local/Temp/**
**/Temp/**
**/Packages/**
*.log
**/*.log

# Build artefacts & local env
.expo
dist
coverage
*.ipa
*.aab
backend
website
docs
tests
scripts
.github
